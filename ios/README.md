# iOS (Capacitor)

Ejecuta `npx cap add ios` después de `npm install` para generar el proyecto nativo dentro de esta carpeta. Luego abre
`ios/App/App.xcworkspace` con Xcode para compilar y firmar la aplicación.

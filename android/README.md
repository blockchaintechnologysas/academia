# Android (Capacitor)

Ejecuta `npx cap add android` después de `npm install` para generar el proyecto nativo dentro de esta carpeta. El comando
creará la estructura completa de Gradle y sincronizará los recursos web desde `dist/`.

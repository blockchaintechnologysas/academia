{
  "name": "academia",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host --port 3324",
    "build": "vite build",
    "preview": "vite preview --host --port 3324",
    "android": "npm run build && npx cap copy android && cd android && ./gradlew assembleDebug",
    "ios": "npm run build && npx cap copy ios && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^5.8.5",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.8.5",
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
